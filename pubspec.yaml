name: search_select
description: "A Flutter dropdown for search select and multi-select functionality widget."
version: 1.0.4
homepage: https://welitonsousa.vercel.app/
repository: https://github.com/welitonsousa/search_select
issue_tracker: https://github.com/welitonsousa/search_select/issues

environment:
  sdk: ^3.6.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
